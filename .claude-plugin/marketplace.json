{
  "name": "jevprune",
  "owner": {
    "name": "ibrahemid"
  },
  "description": "Prunes long Bash results per line before the model reads them, and recovers dropped ranges by run id.",
  "plugins": [
    {
      "name": "jevprune",
      "source": "./",
      "description": "Prunes long Bash results per line before the model reads them, and recovers dropped ranges by run id."
    }
  ]
}
